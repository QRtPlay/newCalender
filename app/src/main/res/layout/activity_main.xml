<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnAddTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Task"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"
        android:onClick="addTaskOnClick"/>

    <Button
        android:id="@+id/btnAddReminder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Reminder"
        android:layout_below="@id/btnAddTask"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"
        android:onClick="addReminderOnClick"/>

    <TextView
        android:id="@+id/tvSleepRecords"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sleep Records"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_below="@id/btnAddReminder"
        android:layout_marginTop="24dp"/>

    <TextView
        android:id="@+id/tvSleepRecordPlaceholder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Placeholder for sleep records"
        android:layout_below="@id/tvSleepRecords"
        android:layout_marginTop="16dp"/>

</RelativeLayout>